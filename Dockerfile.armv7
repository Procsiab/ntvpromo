ARG PYTHON_VERSION=3.9.7-slim-bullseye

# Builder image for generating Python wheels
FROM arm32v7/python:${PYTHON_VERSION} AS builder

COPY qemu-arm-static /usr/bin/
ENV PYTHONUNBUFFERED 1
RUN apt update && apt install -y git gcc g++

WORKDIR /wheels
COPY ./app/requirements.txt /wheels/requirements.txt

RUN pip wheel \
    --requirement ./requirements.txt \
    --index-url https://www.piwheels.org/simple \
    --wheel-dir /wheels


# Final container image for the application
FROM arm32v7/python:${PYTHON_VERSION}
LABEL maintainer "Lorenzo Prosseda <lerokamut@gmail.com>"

COPY --from=builder /usr/bin/qemu-arm-static /usr/bin/
ENV PYTHONUNBUFFERED 1
ENV TZ Europe/Rome
ADD ./app /app
COPY --from=builder /wheels /wheels

RUN pip install --no-cache-dir \
    --requirement /app/requirements-deploy.txt \
    --find-links /wheels \
    && rm -rf /wheels

WORKDIR /app

RUN rm -rf /usr/bin/qemu-arm-static

ENTRYPOINT ["python3", "main.py"]
